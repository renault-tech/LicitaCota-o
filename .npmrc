# Google Drive / drives sincronizados não suportam os symlinks que o pnpm usa
# por padrão. O linker "hoisted" copia os pacotes em vez de criar symlinks.
node-linker=hoisted
prefer-symlinked-executables=false
shamefully-hoist=true
